{
  "name": "analyzer-tree-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:core": "cd core && wasm-pack build --target bundler --out-dir ../extension/src/wasm",
    "compile:extension": "cd extension && npm run compile",
    "watch:extension": "cd extension && npm run watch",
    "build": "npm run build:core && npm run compile:extension"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}